# Frontend env template — copy to "frontend/.env" for local development.
#
# Base URL of the Sentinel API. For `npm run dev`, point at the
# backend (below). Leave unset/empty to call the same origin — correct for the
# built one-URL demo where FastAPI serves the SPA from frontend/dist.
VITE_API_BASE=http://localhost:8000
